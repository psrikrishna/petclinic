version: '3'
services:
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: petclinic

  petclinic:
    build: .
    ports:
      - "8081:8080"
    depends_on:
      - postgres
